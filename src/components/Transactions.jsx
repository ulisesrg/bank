import React from 'react';
import '../assets/styles/components/Transactions.css';

const Transactions = ({ children }) => (
  <section className='transactions'>
    {children}
  </section>
);

export default Transactions;
